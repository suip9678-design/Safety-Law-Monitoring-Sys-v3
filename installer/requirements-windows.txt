# backend/requirements.txt와 내용은 같지만, uvicorn[standard] 대신 순정
# uvicorn만 쓴다. uvicorn[standard]의 uvloop는 유닉스 전용(sys_platform !=
# "win32" 조건)이라 Windows용 wheel이 아예 없는데, `pip download --platform
# win_amd64`는 조건부 의존성(marker)을 "지금 이 빌드 스크립트가 도는
# OS"(Linux) 기준으로 평가해버려서 uvloop를 요구해버리는 pip의 알려진
# 한계가 있다. 이 앱은 웹소켓을 쓰지 않고(--reload도 배포판에서는 사용하지
# 않음) 순정 uvicorn만으로 충분해서, standard 확장 없이 이 파일을 따로 둔다.
fastapi==0.115.0
uvicorn==0.30.6
SQLAlchemy==2.0.35
httpx==0.27.2
python-dotenv==1.0.1
APScheduler==3.10.4
pydantic==2.9.2
truststore==0.9.2
tzdata==2024.2
