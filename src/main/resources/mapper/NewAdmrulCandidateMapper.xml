<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.safetylaw.monitor.mapper.NewAdmrulCandidateMapper">

    <sql id="columns">
        ID, SOURCE_TYPE, EXTERNAL_ID, MASTER_ID, NAME, CATEGORY, DEPARTMENT,
        PROMULGATION_NO, PROMULGATION_DATE, ENFORCEMENT_DATE, DETAIL_LINK,
        MATCHED_KEYWORD, STATUS, FIRST_SEEN_AT
    </sql>

    <insert id="insert" parameterType="com.safetylaw.monitor.domain.NewAdmrulCandidate">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT SEQ_NEW_ADMRUL_CAND.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO NEW_ADMRUL_CANDIDATES (
            ID, SOURCE_TYPE, EXTERNAL_ID, MASTER_ID, NAME, CATEGORY, DEPARTMENT,
            PROMULGATION_NO, PROMULGATION_DATE, ENFORCEMENT_DATE, DETAIL_LINK,
            MATCHED_KEYWORD, STATUS, FIRST_SEEN_AT
        ) VALUES (
            #{id}, #{sourceType}, #{externalId}, #{masterId}, #{name}, #{category}, #{department},
            #{promulgationNo}, #{promulgationDate}, #{enforcementDate}, #{detailLink},
            #{matchedKeyword}, #{status}, #{firstSeenAt}
        )
    </insert>

    <select id="findById" resultType="com.safetylaw.monitor.domain.NewAdmrulCandidate">
        SELECT <include refid="columns"/> FROM NEW_ADMRUL_CANDIDATES WHERE ID = #{id}
    </select>

    <select id="findByStatus" resultType="com.safetylaw.monitor.domain.NewAdmrulCandidate">
        SELECT * FROM (
            SELECT <include refid="columns"/>
              FROM NEW_ADMRUL_CANDIDATES
             WHERE STATUS = #{status}
             ORDER BY FIRST_SEEN_AT DESC, ID DESC
        ) WHERE ROWNUM &lt;= #{limit}
    </select>

    <select id="findAll" resultType="com.safetylaw.monitor.domain.NewAdmrulCandidate">
        SELECT <include refid="columns"/> FROM NEW_ADMRUL_CANDIDATES
    </select>

    <update id="updateStatus">
        UPDATE NEW_ADMRUL_CANDIDATES SET STATUS = #{status} WHERE ID = #{id}
    </update>

    <update id="bulkUpdateStatus">
        UPDATE NEW_ADMRUL_CANDIDATES
           SET STATUS = #{status}
         WHERE ID IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="deleteByIds">
        DELETE FROM NEW_ADMRUL_CANDIDATES
         WHERE ID IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
